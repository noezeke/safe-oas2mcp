# Petstore Demo

This is a small Petstore-style OpenAPI fixture for compatibility testing.

```bash
safe-oas2mcp inspect examples/petstore/openapi.yaml
```

Expected behavior:

- `GET /pets` -> enabled
- `POST /pets` -> confirm

